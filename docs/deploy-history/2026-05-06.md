# 배포 히스토리: 2026-05-06

## Hotfix: kospi200-master-backfill (PR #193, #194 머지)

Phase 8.6 Sprint 2 ATR 캘리브레이션 잡이 3거래일 연속(4/30·5/4·5/6) 폴백 → safe_mode 발동으로 신호 발행 전면 차단된 근본 원인 수정.

**원인 (2단)**:
1. 마이그레이션 `c1f2a30b8201`이 `stocks.is_kospi200` 컬럼만 추가, 모든 row server_default=`false`. 200종을 `true`로 마킹하는 production 코드/잡 부재 → ATR 잡 DB 조회 0종 → 정적 백업 JSON(200종) 폴백.
2. 정적 백업 200종 중 148종은 `market_data` 일봉 미적재 → `coverage_gap=148 ≥ MARKET_DATA_MIN_COVERAGE_GAP(30)` → 데이터 부족 판정 → fallback_count INCR → 3회 누적 → safe_mode.

**브랜치**: `hotfix/kospi200-master-backfill` → main → develop

**변경 파일**:
- `backend/alembic/versions/e5a7c91d4f08_kospi200_master_backfill.py` — 정적 백업 JSON 200종 `is_kospi200=true` UPDATE
- `backend/core/config.py` — `ATR_COVERAGE_GAP_MAX` 환경변수 추가 (default 30, 운영 일시 상향 가능)
- `backend/modules/screening/atr_calibration.py` — `MARKET_DATA_MIN_COVERAGE_GAP` 상수 → `settings.ATR_COVERAGE_GAP_MAX` 동적 조회
- `backend/scripts/diagnose_pipeline.py` — scheduler 네임스페이스 read-only 진단 스크립트
- `backend/tests/test_kospi200_backfill_migration.py` — 마이그레이션 검증
- `.env.example` — `ATR_COVERAGE_GAP_MAX=30` 문서화

**자동 검증**:
- pytest: 1060 passed, 0 failed (10분 18초)
- 타겟 API/Playwright: N/A

**수동 검증 결과 (5/6 KST 09:50~10:00 운영 조치)**:
- Railway alembic 자동 적용: `e5a7c91d4f08 (head)` 확인
- `stocks.is_kospi200=true` 카운트: 52종 (정적 백업 200종 중 production 매칭률 한계 — 후속 핫픽스 `kospi200-real-200-backfill`로 226종 확장)
- Railway 환경변수: `ATR_COVERAGE_GAP_MAX=200` 설정 (선행 임시 상향, KOSPI200 sync 정상화 후 30 원복)
- `safe_mode:active` Redis 키: 삭제 완료
- `metrics:atr:ceil:fallback_count`: '3' → '0' 리셋 완료

**후속 작업**:
- 5/7 ATR 잡 관찰: phase8.6-sprint2-observation-check 에이전트 KST 16:00 재실행 예정
- KOSPI200 200종 갱신 + 누락 일봉 백필: 별도 핫픽스 `kospi200-real-200-backfill` (PR #196, kill switch off로 머지) 등재됨

---

## Hotfix: kospi200-real-200-backfill (PR #196, #197 머지)

선행 핫픽스 `kospi200-master-backfill`의 잔존 부채(정적 백업 placeholder 200종 중 production 매칭 52종 그침, ATR_COVERAGE_GAP_MAX=200 원복 작업 미등재) 해소. KIS `kospi_code.mst` Part2 char position 162 = KOSPI200 멤버십 플래그 검증 후 자동 동기화 잡 신설.

**해결 경로 변경**:
- B안(한투 ETF 구성종목 API): 1회 30종 한계로 부적합 판정
- D안(KIS kospi_code.mst Part2 char position 162) 채택: production mst 226종, spot-check 15/15 적중, false positive 0/4, stocks 매칭 226/226=100%

**브랜치**: `hotfix/kospi200-real-200-backfill` → main (#196) → develop (#197)

**커밋**: `7796219` (구현) + `3956f97` (코드 리뷰 후속: try/except+rollback, deploy.md 형식)

**변경 파일**:
- `backend/core/config.py` — `KOSPI200_MST_SYNC_ENABLED: bool = False` (kill switch, default OFF)
- `backend/modules/collector/sources/kis_master.py` — `parse_kospi200_codes()` / `kospi200_sanity_check()` / `sync_kospi200_membership()` (try/except+rollback) / `_maybe_sync_kospi200()`. ETF mst 다운로드 1회로 양 작업 처리
- `backend/tests/test_kis_master.py` — 10종 추가
- `.env.example` — `KOSPI200_MST_SYNC_ENABLED=false` 문서화
- `docs/hotfix/kospi200-real-200-backfill/hotfix.md` — D안 + kill switch 설계 기록

**자동 검증**:
- pytest tests/test_kis_master.py: 39 passed (10 신규, 0.13초)
- pytest 전체 회귀: 1069 passed, 0 failed (10분 49초)
- 타겟 API/Playwright: N/A — 백엔드 비활성 잡 추가, UI 변경 없음
- 코드 리뷰: 75점 항목 2건(트랜잭션 안전성, deploy.md 형식)을 보강 커밋 `3956f97`로 해소

**배포 헬스 검증 (2026-05-06 KST 15:30 이후, kill switch off 상태)**:
- Railway 자동 배포 성공 — `Application startup complete.` 로그 정상
- 백엔드 health: `https://api.stockbot.choiji.kr/api/v1/health` → 200, healthy, database/redis connected
- 백엔드 readiness: `/api/v1/health/readiness` → 200, scheduler=running, pipeline=healthy
- Railway 환경변수 `KOSPI200_MST_SYNC_ENABLED` 미설정 → default `False` → kill switch off (잡 no-op 보장)
- `ATR_COVERAGE_GAP_MAX=200` 유지 (선행 핫픽스 일시 상향 — KOSPI200 sync 활성화 후 30 원복)
- Application 부팅 import/config 오류 없음

**잔여 (deploy.md 등재)**:
- 5/7 16:00 ATR 잡 관찰 종료 → `KOSPI200_MST_SYNC_ENABLED=true` 토글
- 5/8 08:10 잡 → `KOSPI200 sync 완료: codes=226, marked=226` 확인
- 1~2영업일 정상 후 `ATR_COVERAGE_GAP_MAX=30` 원복
