# 2026-05-11 배포/검증 아카이브

## Hotfix: zero-signal-diagnosis-api (2026-05-11, PR #219 MERGED)

**머지 커밋**: `ab88034`
**머지 시각**: 2026-05-11 13:04 KST
**브랜치**: `hotfix/zero-signal-diagnosis-api` → main → develop (역머지 PR #221)

### 배경

2026-05-11 12:00 KST Phase 8.6 Sprint 3 Paper 1거래일 관찰에서 다음 발견:

- volume_surge dry_run 신호 0건 (05-08, 05-11)
- time_filter 차단 카운터 0건 (morning/afternoon/gap_open 모두)
- R3 auto_rollback **이미 발동** (2026-05-08 16:10 KST, 사유: `auto_rollback_2d_zero_signals`)
- orderbook / vol5m / scheduler 관찰 항목 #2/#3/#6은 **SSH redis-cli 미설치**로 측정 불가

→ 관찰 가시성 부족 → 신호 0건 원인(임계 vs 데이터 vs 스크리닝 vs 롤백 차폐)을 분리할 수 없음.

### 수정 내용

`GET /api/v1/health/sprint3-keys` 신규 엔드포인트:
- `orderbook_count` — `realtime:*:orderbook` 키 개수
- `vol5m_count` — `vol5m:*:YYYYMMDD:*` 오늘 키 개수
- `scheduler.last_portal_supplement` / `last_metrics_rollup` / `last_auto_rollback_check`

### 변경 파일 (3개, 코드 +52줄)

- `backend/api/routes/health.py` (+29)
- `backend/tests/test_health.py` (+23 회귀 테스트)
- `deploy.md` (관찰 결과 + Railway 환경변수 stale 정정)
- `docs/phase/phase8.6/sprint4/threshold_recalibration_hotfix_plan.md` (신규)

### 검증 결과

- ✅ `pytest tests/test_health.py -v` — 4 passed (test_health_sprint3_keys_shape 포함)
- ✅ 프로덕션 응답 검증 (2026-05-11 13:05 KST):
  ```json
  {
    "date": "2026-05-11",
    "orderbook_count": 18,
    "vol5m_count": 1000,
    "scheduler": {
      "last_portal_supplement": null,
      "last_metrics_rollup": "2026-05-10T16:05:03.790772+09:00",
      "last_auto_rollback_check": null
    }
  }
  ```

### 핵심 발견 (배포 후 측정으로 확정)

| 항목 | 결과 |
|------|------|
| 데이터 파이프라인 (orderbook 18 / vol5m 1000) | ✅ 정상 |
| signal 생성 경로 (volume_surge 0, time_filter 차단 0) | ❌ 차단됨 |
| scheduler.metrics_rollup | ✅ 정상 작동 (05-10 16:05) |
| scheduler.portal_supplement | ⚠️ `null` (미적재 — 별도 조사 필요) |
| scheduler.auto_rollback_check | ⚠️ `null` (R3 발동은 했으나 last_run 키 미적재) |

**진단 결론**: 데이터/스케줄러 인프라는 살아있음. 문제는 signal 생성 임계값 또는 스크리닝 로직에 국한 → 단계 C(grid search + 임계 재조정) 우선순위 확정.

### 후속 작업 (`threshold_recalibration_hotfix_plan.md`)

- ⏳ **단계 B 백필 트리거 완료** (2026-05-11, `POST /backtest/backfill-daily` start=2026-02-10 end=2026-05-11)
- ⬜ 단계 C grid search + 임계 재조정 hotfix (B 완료 후)
- ⬜ R3 rollback 자연 해제 (재조정 후 신호 1+건 발생 시)
- ⬜ scheduler.portal_supplement / auto_rollback_check 키 미적재 원인 조사 (별도)

### 역머지

- PR #220 (`main → develop`) — deploy.md 충돌 발생, 닫음
- PR #221 (`chore/hotfix-zero-signal-diagnosis-develop-revmerge → develop`) — 충돌 해결 후 머지 완료

---

## Hotfix: dedup-time-window (2026-05-11, PR #223 MERGED)

**머지 시각**: 2026-05-11 13:35 KST
**브랜치**: `hotfix/dedup-time-window` → main → develop (역머지 PR #224)

### 배경

PR #219 진단 hotfix로 가시성 확보 후 발견된 신호 0건의 진짜 원인:

- Railway 로그 매 30초: `신호 생성: 입력=1, 통과=0 (중복=1)`
- DB `status='pending'` 신호 4건이 영구 잔존 (2026-04-21, 04-24, 05-06, 05-11)
- 원인: `signal_generator.generate_signals()` dedup 쿼리가 **시간 윈도우 없이** `stock_code + status='pending'`만 검사. 텔레그램 토큰이 만료된 후에도 DB row가 `pending` 상태로 영구 잔존 → 해당 종목 영구 차단 → 2거래일 연속 신호 0건 → R3 auto_rollback 발동.

### 변경 내용

1. **`signal_generator.py`**: dedup 쿼리에 `created_at >= now - SIGNAL_DEDUP_WINDOW_HOURS` 절 추가
2. **`config.py`**: `SIGNAL_DEDUP_WINDOW_HOURS` 환경변수 (기본 6시간, 1~72)
3. **`api/routes/trading.py`**: `POST /api/v1/trading/signals/expire-stale?older_than_hours=N` 신규 — admin이 stale pending → expired 일괄 전환
4. **테스트**:
   - `test_dedup_query_uses_time_window` — dedup SQL에 `created_at` 절 포함 검증
   - `test_expire_stale_pending_signals_shape` — 응답 shape 검증

### 검증 결과

- ✅ `pytest tests/test_signal_generator.py tests/test_trading_api.py -v` — 12 passed
- ✅ **전체 회귀 pytest: 1179 passed, 0 failed (11분 12초)**
- ✅ Railway 자동 배포 완료 + 헬스체크 healthy
- ✅ `POST /signals/expire-stale?older_than_hours=24` 호출 → **3건 expired 전환** (id=1, 2, 3)
  - id=4 (2026-05-11 10:15 KST 생성)는 24시간 미초과로 유지 — 6시간 dedup 윈도우상 16:15 KST부터 자동 차단 해제

### scheduler 키 미적재 조사 (#4)

| 키 | 상태 | 결론 |
|----|------|------|
| `last_metrics_rollup` | ✅ 값 존재 (비거래일에도 실행) | 정상 |
| `last_portal_supplement` | ⚠️ null | **버그 아님** — `is_trading_day` 비거래일 즉시 return + STATE_TTL=24h → 마지막 거래일(05-08 16:00) 이후 자연 만료 |
| `last_auto_rollback_check` | ⚠️ null | **의도된 동작** — `_check_auto_rollback`에 setter 미호출. 가시성 개선용 별도 chore 후보 (필수 아님) |

### 백필 광범위 재트리거 (#3)

- 좁은 범위 (02-10~02-20) 백필 검증 완료 — Railway 로그에 `kis_daily_collector` 활동 확인
- **광범위 (02-21~05-11) 백필 트리거** (2026-05-11 13:35 KST, 202 Accepted, BackgroundTasks)
- KIS rate limit (500 calls/day): 200종목 × ~50 거래일 = 10,000건 → 약 24h+ 소요 예상
- 완료 후 walk-forward (n_days=60) 재실행 → grid search 가능 여부 재평가

### 단계 C hotfix-close 종합

✅ **단계 A** — 진단 API (PR #219) 완료
✅ **단계 B** — 백필 트리거 (2개 범위 진행 중)
✅ **단계 C** — dedup 시간 윈도우 + stale 정리 (PR #223) 완료

남은 모니터링:
- ⬜ Paper 관찰 재시작: volume_surge dry_run 신호 ≥1건 발생 확인
- ⬜ R3 auto_rollback 자연 해제 (2거래일 연속 신호 ≥1건 시)
- ⬜ id=4 신호 16:15 KST 이후 자동 차단 해제 확인
- ⬜ 백필 완료 후 walk-forward 60일 결과 재측정
