# 배포 이력 — 2026-04-21

## 핫픽스: 일일 리스크 카운터 자동 리셋 누락 (2026-04-21)

PR: https://github.com/frogy95/stockbot/pull/153

### 배경
2026-04-21 장중 관찰 중 실거래 모드에서 momentum_breakout 신호(036830, 10:00 KST)가 DB pending으로 저장되었으나 텔레그램 승인 요청 미발송. 리스크 상태 페이지에서 연속 손절 카운터=3 확인 (매매 이력 전무).

### 원인
`reset_daily_counters()` 메서드가 정의·단위 테스트만 있고 프로덕션 어디서도 호출되지 않음 → `risk:consecutive_loss_count` Redis 키 영구 누적 → `check_consecutive_loss()` 영구 True 반환 → 모든 매매 신호 차단.

### 수정
- `backend/modules/collector/scheduler.py` `_market_open()`에서 `reset_daily_counters()` 호출 wiring (매 거래일 09:00 자동 리셋)
- `backend/api/routes/trading.py`에 `POST /api/v1/trading/risk/reset` 관리자 엔드포인트 추가

### 자동 검증
- ✅ pytest: `test_risk_daily_capital + test_scheduler_vol5m` 10 passed
- ✅ Railway 헬스체크: healthy (DB/Redis 연결)
- ✅ 엔드포인트 등록 확인: `POST /api/v1/trading/risk/reset` HTTP 401 (인증 필요)

### 수동 조치 필요
- ⬜ JWT로 `POST /api/v1/trading/risk/reset` 1회 호출 — 현재 카운터 3/쿨다운 즉시 해제
- ⬜ 2026-04-22 09:00 자동 리셋 로그 확인
